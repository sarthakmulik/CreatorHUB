Massive Instagram build for CreatorHub, in 4 reviewable stages. Priority (per user): Deep Insights + Best-Time Heatmap + Real Comment CRM, plus Hindi/Hinglish/English AI captions.

STAGE 1 — Deep IG Insights + Demographics
- models.py: add PostInsight (JSONB per-post metrics), AudienceSnapshot (daily age/gender/cities/countries); add reach/impressions/saves/shares/replies to DailySnapshot
- schema.sql: append new tables + ALTER statements (matches repo's manual-SQL workflow — no alembic/versions dir exists)
- instagram_service.py: rewrite fetch_insights() to pick metric sets by media_product_type (REELS vs FEED vs STORY) — reach, impressions, saves, shares, replies, profile_visits, exits, taps; add fetch_audience_demographics() via audience_city/country/gender_age
- routers/instagram.py: new GET /insights/{post_id}, /audience/{account_id}
- Frontend: new /(dashboard)/instagram page — audience cards (gender/age pie, top cities/countries), per-post deep metrics table, engagement-quality ring; extend VideoTable for IG saves/shares/reach

STAGE 2 — Best-Time-to-Post Heatmap (IST)
- models.py: add AudienceOnline (hour_ist 0-23, weight)
- instagram_service.py: fetch_audience_online(), normalize UTC→IST (+5:30)
- routers/instagram.py: GET /best-time/{account_id} → 24-pt array + top-3 slots
- Frontend: BestTimeHeatmap.tsx (Recharts/CSS, no new deps), recommended-time chip that pre-fills CreatePostModal time field

STAGE 3 — Real Comment CRM (replace hardcoded mocks)
- instagram_service.py: fetch_ig_comments() via /{media_id}/comments, store real Comment rows; wire into sync_instagram_account + 5-min sync_all_accounts
- ai_tasks.py: REMOVE hardcoded mock comments, pull real Comment rows, run existing VADER, extend categorization regex with Hinglish (collab|sponsor|brand|price|kitna|rate)
- routers/crm.py: /comments/sync/{account_id}, /ai-analyze
- crm/page.tsx: existing UI already polished — just gets real data; wire dead 'Auto-Draft Reply' button to Gemini reply generator

STAGE 4 — Reliability + AI Captions (Hindi/Hinglish/English)
- instagram_service.py: token refresh via fb_exchange_token; scheduler.py: daily job refreshing tokens <7 days from expiry (prevents silent 60-day publishing death)
- routers/ai.py: /ai/caption using already-wired Gemini → 3 caption variants (Hindi/Hinglish/English) + 15-30 niche hashtags, tuned for Indian Reels
- CreatePostModal.tsx: '✨ Generate' button

Approach notes:
- No new heavy deps — Graph API via existing httpx, Recharts already installed, VADER already in requirements.txt
- Graph API v19.0 kept consistent with existing calls
- Schema changes delivered as raw SQL statements to run (repo has no alembic dir)
- Indian focus: IST normalization, city demographics, Hinglish keyword + caption tuning